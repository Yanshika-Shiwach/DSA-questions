
class Solution {
  public:
    bool isPerfect(vector<int> &arr) {
        // code here
        int i=0,j=arr.size()-1;
        while(i<j){
            if(arr[i]!=arr[j])
          return false;
          
        
        j--;
        i++;
        }
        return true;
    }
};
