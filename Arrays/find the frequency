class Solution {
  public:
    int findFrequency(vector<int> arr, int x) {
        // Your code here
        int n=arr.size();
        int cnt=0;
        for(int i=0;i<n;i++){
            if(arr[i]==x)cnt++;
        }
        return cnt;
    }
};
