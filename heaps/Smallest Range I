class Solution {
public:
    int smallestRangeI(vector<int>& nums, int k) {
        int n=nums.size();
        sort(nums.begin(),nums.end());
        int start=nums[0]+k;
        int end=nums[n-1]-k;
        if(end>start)
        return end-start;
        else return 0;
    }
};
