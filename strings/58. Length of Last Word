class Solution {
public:
    int lengthOfLastWord(string s) {
       int n=s.size();
       int j=0;
       for(int i=n-1;i>=0;i--){
        if(s[i]==' '&&j==0)continue;
        if(s[i]!=' ')j++;
        else break;
       } 
       return j;
    }
};
